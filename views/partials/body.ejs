<!DOCTYPE html>
<html lang="en">
    <head>
        <style>
            <%- include('../stylesheets/app.css'); %>
        </style>
    </head>
    <body>
        <h2>Body part</h2>
        <p>
            Lorem ipsum dolor sit amet consectetur adipisicing elit. Iste,
            aspernatur. <%= submit.submit(); %>
        </p>
        <p>Some other Lorem ipsum dolor sit amet.</p>
        <select id="select_button">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
        </select>
        <ons-click>
            <button id="submit_button">Submit</button>
        </ons-click>
        <script>
            const button = document.getElementById("submit_button");
            let value = 1;

            button.addEventListener("click", () => {
                console.log(value);
            });
            const select = document.getElementById("select_button");
            select.addEventListener("change", (item) => {
                value = item.target.value;
            });
        </script>
    </body>
</html>
