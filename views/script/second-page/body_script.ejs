<script>
    let inputName;
    let inputPassword;

    function getName(event) {
        let name = event.target.value;
        inputName = name;
    }
    function getPassword(event) {
        let password = event.target.value;
        inputPassword = password;
    }
    function submitHandler(event) {
        event.preventDefault();
		postData("http://localhost:8000/second-page",{username:  inputName, password: inputPassword})
    }
	function postData(url, data)
	{
		fetch(url=url,{method: "POST", body : JSON.stringify({username: "Some user", password: "some password"})}).then(response => console.log(response)).catch((error)=>console.log({message:error.message}))
	}
</script>
